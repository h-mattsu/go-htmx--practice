# go-htmx-practice

Minimal Go project demonstrating layered architecture and a tiny htmx-enabled example.

Run the server:

```bash
go run main.go
```

Open http://localhost:8080
# go-htmx--practice
